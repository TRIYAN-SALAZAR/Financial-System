#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
#include <windows.h>
#include "librerias/data_of_users.h"

int main(){
    int i;
    for (i = 0; i < nips[i]; i++) {
        printf("\nNip %d: %d", i, nips[i]);
    }

    getch();
    return 0;
}
